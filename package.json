{
  "name": "veta",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "typecheck": "pnpm -r --no-bail run typecheck",
    "test": "node --test tests/*.test.mjs && pnpm -r --no-bail run test",
    "build": "pnpm -r --no-bail run build",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
