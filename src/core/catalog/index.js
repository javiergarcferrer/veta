// core/catalog — the brand-catalog ViewModels barrel. Import the Model from
// here (like core/quote, core/tracking, core/accounting), not from the files.
export { lsgModelTitle, groupLsgModels, resolveLsgCatalogBook } from './lsgBook.js';
export { resolveCatalogSearch, modelDescription } from './search.js';
